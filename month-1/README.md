# Arch Technologies — Month 1

| Task | Folder | How to run |
|------|--------|------------|
| 1 · Streamlit + local Ollama | `task-1-streamlit-ollama/` | `streamlit run app.py` (Ollama must be running) |
| 2 · Medical QLoRA (Unsloth) | `task-2-medical-qlora-unsloth/` | Colab notebook, **T4 GPU**, Runtime → Run all |

Do not run the Unsloth notebook on a CPU-only Windows install; it needs a CUDA GPU (Colab is the intended environment).
